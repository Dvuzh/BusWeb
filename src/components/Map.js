import {Component} from "react";
import React from "react";

class Map extends Component {
    render() {
        return (
            <section>
                <div className="container">
                    <button type="button" className="btn-map"> Карта</button>
                </div>
            </section>

        );
    }
}

export default Map;